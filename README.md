# Portfolio - Phạm Thu Hường

Digital Portfolio cho sinh viên Luật Thương Mại Quốc Tế

## 📋 Cấu trúc thư mục

```
portfolio/
├── index.html
├── package.json
├── vite.config.js
├── tailwind.config.js
├── postcss.config.js
├── src/
│   ├── main.jsx
│   ├── Portfolio.jsx
│   └── index.css
└── README.md
```

## 🚀 Hướng dẫn cài đặt

### Bước 1: Cài đặt Node.js

Đảm bảo bạn đã cài đặt Node.js (phiên bản 18 trở lên)

-   Tải tại: https://nodejs.org/

### Bước 2: Tạo thư mục dự án

```bash
mkdir portfolio
cd portfolio
```

### Bước 3: Tạo các file

1. **Tạo file `package.json`** (copy nội dung từ artifact)
2. **Tạo file `vite.config.js`** (copy nội dung từ artifact)
3. **Tạo file `tailwind.config.js`** (copy nội dung từ artifact)
4. **Tạo file `postcss.config.js`** (copy nội dung từ artifact)
5. **Tạo file `index.html`** (copy nội dung từ artifact)

6. **Tạo thư mục `src/` và các file bên trong:**

```bash
mkdir src
```

-   `src/main.jsx` (copy nội dung từ artifact)
-   `src/Portfolio.jsx` (copy component React từ artifact đầu tiên)
-   `src/index.css` (copy nội dung từ artifact)

### Bước 4: Cài đặt dependencies

```bash
npm install
```

### Bước 5: Chạy development server

```bash
npm run dev
```

Website sẽ tự động mở tại: `http://localhost:3000`

## 📦 Các lệnh có sẵn

-   `npm run dev` - Chạy development server
-   `npm run build` - Build production
-   `npm run preview` - Preview bản build
-   `npm run lint` - Check code quality

## 🎨 Công nghệ sử dụng

-   **React 18** - UI Library
-   **Vite** - Build tool nhanh
-   **Tailwind CSS** - Utility-first CSS framework
-   **Lucide React** - Icon library
-   **PostCSS** - CSS processing

## 📱 Tính năng

✅ Responsive design (Mobile, Tablet, Desktop)
✅ Smooth animations & transitions
✅ Gradient theme đẹp mắt
✅ Navigation menu (desktop & mobile)
✅ 3 trang: Giới thiệu, Dự án, Tổng kết
✅ Custom scrollbar với gradient
✅ Hover effects trên cards
✅ Fast loading với Vite

## 🔧 Tùy chỉnh

-   **Màu sắc**: Chỉnh trong `tailwind.config.js`
-   **Nội dung**: Chỉnh trong `src/Portfolio.jsx`
-   **Styles**: Chỉnh trong `src/index.css`

## 📄 License

© 2026 Phạm Thu Hường - Digital Portfolio

---

**Lưu ý**: Đảm bảo tất cả các file được đặt đúng vị trí theo cấu trúc thư mục ở trên.
